import React from 'react'
import styles from './Auth.module.scss'

type LoginFormProps = {

}

export const LoginForm:React.FC<LoginFormProps> = ({}) => {
  return (
    <div className={styles.root}>LoginForm</div>
  )
}